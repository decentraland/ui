{
  "buildCommand": "npm run build:lib && npm run build:storybook && cp ./lib/*.css ./storybook-static",
  "outputDirectory": "storybook-static",
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  }
}
